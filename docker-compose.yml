services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-admin123}
      - ENVIRONMENT=${ENVIRONMENT:-DEV}
      - DATABASE_URL=${DATABASE_URL:-sqlite:///./groups.db}
      - ROOT_PATH=${ROOT_PATH:-}
    restart: unless-stopped
